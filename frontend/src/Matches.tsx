import React,{useEffect,useMemo,useState}from'react';
import{Search}from'lucide-react';
import{supabase,supabaseConfigError}from'./supabase';
import ClubCrest from'./ClubCrest';
type MatchRow={match_id:number;match_date:string;season:string|null;opponent:string;opponent_crest_url:string|null;competition:string;round:string|null;venue_type:string;leeds_score:number;opponent_score:number;result:string;stadium:string|null;attendance:number|null;leeds_manager:string|null;opposition_manager_name:string|null;first_goal:string|null};
type VenueFilter='All'|'Home'|'Away'|'Neutral';
const venueLabel=(v:string)=>v==='H'?'Home':v==='A'?'Away':'Neutral';
const resultCode=(r:string)=>r==='Won'?'W':r==='Draw'?'D':'L';
const day=(d:string)=>new Date(`${d}T00:00:00`).toLocaleDateString('en-GB',{weekday:'short'});
const venues:VenueFilter[]=['Home','Away','Neutral'];
const competitions=['Associate Members Cup','Champions League','Championship','Division One','Division Two','European Cup',"European Cup Winners' Cup",'FA Charity Shield','FA Cup','Full Members Cup','Inter-City Fairs Cup','League Cup','League One','Play-Off','Premier League','UEFA Cup'];
const competitionDataValue=(v:string)=>v==='Associate Members Cup'?'Football League Trophy':v==='Play-Off'?'Play-Offs':v;
function FormBadge({result}:{result:'W'|'D'|'L'}){return <span className={`form-pill form-${result.toLowerCase()}`}>{result}</span>}
export default function Matches(){const[rows,setRows]=useState<MatchRow[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(supabaseConfigError),[search,setSearch]=useState(''),[competition,setCompetition]=useState('All Comps'),[venue,setVenue]=useState<VenueFilter>('All'),[opponent,setOpponent]=useState('All Opponents');useEffect(()=>{if(!supabase){setLoading(false);return}setLoading(true);setError(null);supabase.from('match_centre_summary').select('match_id,match_date,season,opponent,opponent_crest_url,competition,round,venue_type,leeds_score,opponent_score,result,stadium,attendance,leeds_manager,opposition_manager_name,first_goal').order('match_date',{ascending:false}).order('match_id',{ascending:false}).range(0,499).then(({data,error})=>{if(error)setError(error.message);else setRows((data??[])as MatchRow[]);setLoading(false)})},[]);const opponents=useMemo(()=>Array.from(new Set(rows.map(r=>r.opponent).filter(Boolean))).sort((a,b)=>a.localeCompare(b)),[rows]);const visible=useMemo(()=>{const q=search.trim().toLowerCase();return rows.filter(r=>(competition==='All Comps'||r.competition===competitionDataValue(competition))&&(venue==='All'||venueLabel(r.venue_type)===venue)&&(opponent==='All Opponents'||r.opponent===opponent)&&(!q||r.opponent.toLowerCase().includes(q)||r.competition.toLowerCase().includes(q)||(r.leeds_manager??'').toLowerCase().includes(q)||(r.opposition_manager_name??'').toLowerCase().includes(q)||(r.stadium??'').toLowerCase().includes(q)||(r.season??'').toLowerCase().includes(q)||(r.round??'').toLowerCase().includes(q)||(r.first_goal??'').toLowerCase().includes(q)))},[rows,search,competition,venue,opponent]);return <><div className="card lb-table-card"><div className="lb-table-header"><div className="lb-title-group"><span className="section-kicker">Leeds United history</span><h2>Matches</h2><p>Leeds United match archive</p></div><div className="section-kicker lb-match-count">{loading?'…':visible.length.toLocaleString('en-GB')} Matches Shown</div></div><div className="lb-filterbar"><div className="lb-filter-section"><span className="lb-filter-label">Competition</span><select className="lb-filter-select" value={competition} onChange={e=>setCompetition(e.target.value)} aria-label="Competition"><option value="All Comps">All Comps</option>{competitions.map(c=><option key={c} value={c}>{c}</option>)}</select></div><div className="lb-filter-section lb-venue-section"><span className="lb-filter-label">Venue</span><div className="lb-filter-pills"><button className={`lb-filter-pill ${venue==='All'?'active':''}`} onClick={()=>setVenue('All')}>All</button>{venues.map(v=><button key={v} className={`lb-filter-pill ${venue===v?'active':''}`} onClick={()=>setVenue(v)}>{v}</button>)}</div></div><div className="lb-filter-section"><span className="lb-filter-label">Opponent</span><select className="lb-filter-select" value={opponent} onChange={e=>setOpponent(e.target.value)} aria-label="Opponent"><option value="All Opponents">All Opponents</option>{opponents.map(o=><option key={o} value={o}>{o}</option>)}</select></div><label className="lb-search matches-search"><Search size={14}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search matches"/></label></div>{loading?<div className="lb-loading">Loading matches from the Lufcdatabase…</div>:error?<div className="lb-loading"><strong>Data connection error:</strong> {error}</div>:<div className="lb-table-wrap"><table className="lb-table matches-table"><thead><tr><th>Match</th><th>Season</th><th>Day</th><th>Date</th><th>Opponent</th><th>Venue</th><th>Competition</th><th>R</th><th>Form</th><th>Score</th><th>Stadium</th><th>First Goal</th><th>Attendance</th><th>Leeds Manager</th><th>Opposition Manager</th></tr></thead><tbody>{visible.map(r=>{const form=resultCode(r.result) as'W'|'D'|'L';return <tr key={r.match_id} style={{height:44}}><td className="metric-value match-number">#{r.match_id}</td><td>{r.season??'—'}</td><td>{day(r.match_date)}</td><td className="metric-value">{new Date(`${r.match_date}T00:00:00`).toLocaleDateString('en-GB')}</td><td><div className="team-cell"><ClubCrest crestUrl={r.opponent_crest_url} name={r.opponent}/><span className="team-name">{r.opponent}</span></div></td><td>{venueLabel(r.venue_type)}</td><td>{r.competition}</td><td className="metric-value">{r.round??'—'}</td><td><FormBadge result={form}/></td><td className="metric-value">{r.leeds_score}–{r.opponent_score}</td><td>{r.stadium??'—'}</td><td>{r.first_goal??'—'}</td><td className="metric-value">{r.attendance==null?'—':r.attendance.toLocaleString('en-GB')}</td><td>{r.leeds_manager??'—'}</td><td>{r.opposition_manager_name??'—'}</td></tr>})}</tbody></table>{!visible.length&&<div className="lb-empty">No matches match these filters.</div>}</div>}</div><div className="card lb-legend"><div className="lb-legend-items"><span>Showing the 500 most recent fixtures in this first synced Matches view.</span><span>R Cup round</span><span>First Goal Scored / Conceded / None</span><span>W Win</span><span>D Draw</span><span>L Loss</span></div></div></>}
